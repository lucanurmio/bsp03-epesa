# The mbox file generated by either of the 4 variations of Epesa whenever retrieving emails.
localMailbox.mbox
# The PKCS7 file that is generated whenever "appSmail.py" is used to retrieve emails. It is used as an intermediary storage to store the latest email before it is decrypted.
encrypt.p7
